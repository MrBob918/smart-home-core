version: '3.8'

services:
  core:
    build:
      context: ./core
      dockerfile: Dockerfile
    container_name: smart-home-core
    ports:
      - "8080:8080"
    volumes:
      - ./core:/app
    environment:
      - ENV=development
    restart: unless-stopped
